Keyword	Description

abstract	Defines an abstract class or method that must be implemented in derived classes.
as	Used for casting an object to a specific type.
base	Refers to the base class of the current instance.
bool	Represents a Boolean value (true or false).
break	Exits from a loop or switch statement.
byte	Represents an 8-bit unsigned integer.
case	Defines a branch in a switch statement.
catch	Declares a block of code, known as an exception handler, that can handle exceptions thrown by try blocks.
char	Represents a single 16-bit Unicode character.
checked	Enables overflow checking for integral types.
class	Declares a class.
const	Declares a constant field or local variable.
continue	Skips the current iteration of a loop and proceeds to the next iteration.
decimal	Represents a 128-bit precise decimal value with 28-29 significant digits.
default	Specifies the default value of a type or the default case in a switch statement.
delegate	Declares a delegate type, which is a reference type that can encapsulate methods with specific signatures.
do	Starts a do-while loop that executes at least once before checking its condition.
double	Represents a double-precision floating-point number.
else	Specifies an alternative block of code to execute if the condition in an if statement is false.
enum	Declares an enumeration, which is a distinct type consisting of a set of named constants.
event	Declares an event, which is a message sent by an object to signal the occurrence of an action.
explicit	Indicates that a conversion operator requires an explicit cast to be invoked.
extern	Indicates that the method is implemented externally (e.g., in another assembly).
false	Represents the Boolean value false.
finally	Declares a block of code that will execute after try and catch blocks, regardless of whether an exception was thrown.
fixed	Pins a variable in place so that it can be accessed by pointer operations without being moved by garbage collection.
float	Represents a single-precision floating-point number.
for	Initiates a for loop, which executes statements repeatedly based on specified conditions.
foreach	Iterates through each element in a collection or array.
goto	Transfers control to another part of the program, identified by a label.
if	Executes statements based on whether a specified condition evaluates to true or false.
implicit	Indicates that a conversion operator can be invoked without an explicit cast.
in	Used in foreach loops and specifies input parameters in method signatures (in parameter).
int	Represents a signed 32-bit integer type.
interface	Declares an interface, which is a contract that classes can implement.
internal	Specifies that access is limited to the current assembly.
is	Checks if an object is compatible with a given type and performs casting if it is compatible.
lock	Acquires the mutual-exclusion lock for a specified object, preventing other threads from accessing it until released.
long	Represents a signed 64-bit integer type.
namespace	Declares a scope that contains a set of related objects, such as classes and interfaces.
new	Creates objects or hides members inherited from base classes; also used to declare constructors.
null	Represents the absence of any object or value; it is not initialized yet.
object	The base type from which all other types derive; represents any data type in C#.
operator	Defines user-defined operators for custom types or overloads existing operators for types defined by the user.
out	Indicates that an argument is passed by reference and will be initialized within the called method.
override	Modifies the implementation of an inherited member from the base class.
params	Allows you to specify a variable number of arguments for methods.
private	Restricts access to members within the containing class only.
protected	Restricts access to members within derived classes and containing classes.
public	Allows access to members from any other code.
readonly	Indicates that fields can only be assigned during declaration or within constructors.
ref	Indicates that an argument is passed by reference.
return	Exits from the current method and optionally returns a value.
sbyte	Represents an 8-bit signed integer.
sealed	Prevents other classes from inheriting from it.
short	Represents a signed 16-bit integer.
sizeof	Returns the size in bytes of any data type.



List of Contextual Keywords

eyword	Description
add	Defines an add accessor for events or properties.
alias	Used to create an alias for namespaces or types.
async	Marks methods as asynchronous, allowing them to use await expressions.
await	Pauses execution until the awaited asynchronous operation completes.
dynamic	Bypasses compile-time type checking; allows dynamic binding at runtime.
from	Used in LINQ queries to specify data sources.
get	Defines accessors for properties; retrieves values.
global	Refers to global namespace; allows access outside local namespaces.
group	Used in LINQ queries to group elements based on keys.
into	Used in LINQ queries to create temporary variables for further operations.
join	Combines elements from two sequences based on matching keys in LINQ queries.
let	Introduces range variables in LINQ queries for intermediate calculations.
nameof	Returns the name of variables, types, or members as strings at compile time.
partial	Indicates that classes, methods, or properties can be split across multiple files.
remove	Defines remove accessors for events or properties.
select	Projects each element into new forms in LINQ queries.
set	Defines accessors for properties; sets values.
value	Refers to the value being assigned in property setters.
var	Implicitly typed local variables; allows type inference by compiler.
when	Used in pattern matching and switch expressions; specifies conditions for matches.
where	Specifies constraints on generic types; used in LINQ queries for filtering results.
yield	Used in iterator methods to return each element one at a time.
